jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # ... (チェックアウト、依存関係のインストール、静的ファイルの生成など)

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dist # 公開ディレクトリを指定 (通常は dist)